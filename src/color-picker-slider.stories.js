import Picker from './color-picker-slider';
export default {
  title: 'Color picker slider',
};

const Template = (args) => `<color-picker-slider></color-picker-slider>`;

export const Primary = Template.bind({});
